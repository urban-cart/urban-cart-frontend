import { ProductsList } from "../components/ProductsList";
import { Layout } from "../components/Layout";

export const Home = () => {
  return (
    <div>

    <Layout>
        <ProductsList />
    </Layout>

    </div>
  );
};
